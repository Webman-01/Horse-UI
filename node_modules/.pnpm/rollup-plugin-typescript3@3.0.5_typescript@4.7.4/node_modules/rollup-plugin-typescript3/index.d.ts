import { Plugin } from "rollup";
import ts from "typescript";
interface PluginOptions {
    compilerOptions: ts.CompilerOptions;
}
declare function typescript(options?: PluginOptions): Plugin<any>;
export = typescript;
